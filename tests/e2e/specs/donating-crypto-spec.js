before(() => {
  cy.setupMetamask('lawn sheriff burst few list scale crash dynamic orient fetch useless spot', 'goerli', 'd.I$-R#Mxa8')
})

describe('Start donation', () => {
  it('login', () => {
    cy.visit('https://dev.artizen.fund/grants/today')
  })
})
