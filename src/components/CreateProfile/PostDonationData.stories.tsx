import CreateProfile from './'

export default {
  title: 'components/CreateProfile',
  component: CreateProfile,
  argTypes: {},
}

export const CreateProfileComponent = (props: any) => <CreateProfile {...props} />
