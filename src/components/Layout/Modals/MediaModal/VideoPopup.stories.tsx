import MediaModal from './'

export default {
  title: 'components/MediaModal',
  component: MediaModal,
  argTypes: {},
}

export const MediaModalComponent = (props: any) => <MediaModal {...props} />
