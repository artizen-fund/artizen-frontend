import TileControl from './'

export default {
  title: 'components/TileControl',
  component: TileControl,
  argTypes: {},
}

export const TileControlComponent = (props: any) => <TileControl {...props} />
