import CloseButton from './'

export default {
  title: 'components/CloseButton',
  component: CloseButton,
  argTypes: {},
}

export const CloseButtonComponent = (props: any) => <CloseButton {...props} />
