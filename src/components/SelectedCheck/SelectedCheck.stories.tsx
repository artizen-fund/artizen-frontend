import SelectedCheck from './'

export default {
  title: 'components/SelectedCheck',
  component: SelectedCheck,
  argTypes: {},
}

export const SelectedCheckComponent = (props: any) => <SelectedCheck {...props} />
