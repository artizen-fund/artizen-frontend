import AmountWidget from './'

export default {
  title: 'components/AmountWidget',
  component: AmountWidget,
  argTypes: {},
}

export const AmountWidgetComponent = (props: any) => <AmountWidget {...props} />
