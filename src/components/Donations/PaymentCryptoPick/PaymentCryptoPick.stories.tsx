import PaymentCryptoPick from './'

export default {
  title: 'components/PaymentCryptoPick',
  component: PaymentCryptoPick,
  argTypes: {},
}

export const PaymentCryptoPickComponent = (props: any) => <PaymentCryptoPick {...props} />
