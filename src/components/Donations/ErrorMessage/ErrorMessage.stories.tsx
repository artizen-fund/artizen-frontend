import ErrorMessage from './'

export default {
  title: 'components/ErrorMessage',
  component: ErrorMessage,
  argTypes: {},
}

export const ErrorMessageComponent = (props: any) => <ErrorMessage {...props} />
