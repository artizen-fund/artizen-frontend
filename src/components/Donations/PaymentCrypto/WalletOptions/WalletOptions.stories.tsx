import WalletOptions from './'

export default {
  title: 'donations/WalletOptions',
  component: WalletOptions,
  argTypes: {},
}

export const WalletOptionsComponent = (props: any) => <WalletOptions {...props} />
