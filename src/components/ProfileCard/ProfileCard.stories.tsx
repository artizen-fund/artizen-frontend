import ProfileCard from './'

export default {
  title: 'components/ProfileCard',
  component: ProfileCard,
  argTypes: {},
}

export const ProfileCardComponent = (props: any) => <ProfileCard {...props} />
